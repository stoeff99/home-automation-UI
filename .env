# Flask
FLASK_DEBUG=true

# Loxone (placeholder, not yet used)
LOXONE_HOST=192.168.20.2 
LOXONE_USER=HA
LOXONE_PASSWORD=%Daniela2025

# MQTT
MQTT_BROKER_HOST=mosquitto
MQTT_BROKER_PORT=1883

# InfluxDB – after first launch, open http://localhost:8086 to set up
INFLUX_URL=http://influxdb:8086
INFLUX_TOKEN=my-token               # replace with real one after setup
INFLUX_ORG=home
INFLUX_BUCKET=sensors
